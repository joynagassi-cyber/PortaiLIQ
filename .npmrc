# @cloudflare/next-on-pages declares a peer range of next >=14.3,
# while the project pins next ^14.2.0. Keep installing with
# legacy-peer-deps so `npm ci` / `npm install` work on fresh checkouts.
legacy-peer-deps=true
